# Domain docs

Este é um repositório de contexto único. Antes de explorar ou alterar código, leia `CONTEXT.md` e as ADRs relevantes em `docs/adr/`.

Use o vocabulário definido em `CONTEXT.md` e sinalize conflitos com ADRs existentes.
